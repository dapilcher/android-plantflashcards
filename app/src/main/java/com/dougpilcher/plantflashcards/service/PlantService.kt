package com.dougpilcher.plantflashcards.service

import com.dougpilcher.plantflashcards.dto.Plant

class PlantService {

    fun parsePlantsFromJsonData(difficulty : String?) : List<Plant>? {
        // declare our return type
        var allPlants = ArrayList<Plant>()
        // Open connection to data feed

        // Parse JSON results to Plant objects

        // Add Plant objects to collection

        // return collection
        return allPlants
    }

}

